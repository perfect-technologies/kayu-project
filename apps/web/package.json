{
  "name": "@kayu/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo 'web scaffold ready'",
    "build": "tsc -p tsconfig.json --noEmit",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'no linter configured'",
    "clean": "rm -rf .next dist"
  },
  "dependencies": {
    "@kayu/api": "workspace:*",
    "@kayu/schemas": "workspace:*",
    "@kayu/ui": "workspace:*",
    "@kayu/utils": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
