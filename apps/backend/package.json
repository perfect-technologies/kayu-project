{
  "name": "@kayu/backend",
  "version": "0.1.0",
  "private": true,
  "main": "./src/main.ts",
  "scripts": {
    "dev": "echo 'backend scaffold ready'",
    "build": "tsc -p tsconfig.json --noEmit",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'no linter configured'",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@kayu/schemas": "workspace:*",
    "@kayu/utils": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
